{
  "name": "instagram-publisher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Publishes due posts from post_queue to Instagram via the Graph API — no third-party posting service.",
  "scripts": {
    "publish": "node publish.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
